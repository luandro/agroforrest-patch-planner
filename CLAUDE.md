# Agent Handoff Notes

- Start with `AGENTS.md` for contributor norms and quick links into `./context/`.
- Each numbered file under `./context/` summarizes a major module (app shell, layout kit, canvas engine, patch workflow, state/storage, build tooling).
- Before coding, open the relevant context brief to understand entry points, key files, and caveats. Update or add briefs if you create new modules.
- Keep this file synced with `AGENTS.md` whenever you rename or add context docs so downstream agents do not miss updates.
